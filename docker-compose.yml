services:
  webchatroom:
    build:
      context: ./WebChatRoom
      dockerfile: Dockerfile
    container_name: webchatroom
    restart: always
    networks:
      - appnet
    ports:
      - "80:80"
    environment:
      - ASPNETCORE_URLS=http://+:80


networks:
  appnet:
    external: true